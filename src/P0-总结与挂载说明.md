# P0 交付总结 + 挂载说明

> P0 目标：宿主地基。四个交付物已完成骨架，待挂载到 DSH 前需完成"待查证项"。

## 交付物清单

| # | 交付物 | 路径 | 状态 |
|---|---|---|---|
| P0-1 | 技能库能力清单 | `skills/capability-registry.json` | ✅ 完成（8 条能力登记） |
| P0-2 | 三张表投影设计文档 | `src/projection/README.md` | ✅ 完成 |
| P0-2b | 三张表投影 JS 骨架 | `src/projection/projections.js` | ✅ 完成（schema/apply/view） |
| P0-3 | 阿极 preset 组成草案 | `preset/README.md` | ✅ 完成（persona+白帽+goal契约） |
| P0-4 | 宿主插件代码骨架 | `src/host/host-plugin.js` | ✅ 完成（投影+技能注册） |
| P0-5 | 本总结 + 挂载说明 | 本文件 | ✅ 完成 |

## 关键设计（已落实）

1. **三张表投影**：需求表（阿极）、任务表（参谋部，甘特图式）、官员建议表（官员，零修改权）。事件驱动纯函数折叠，参考 dsh-goal 真实写法。
2. **稀疏投影**：view 只输出"框架 + 当前项"，但精确实现待挂载后对照 view 契约。
3. **阿极 persona**：含七条铁律 + 澄清原则（能推断不问、有歧义才问）。
4. **白帽义务**：方案确认前必输出反方视角。
5. **goal 契约**：开工前锁 scope/target/finish/out-of-scope/constraints/evidence。
6. **能力路由**：capability-registry.json 的 triggers 是确定性路由的数据源。

## 挂载前必做的待查证项（铁律七：不臆断）

挂载到 DSH 前，以下【占位/待查证】内容必须用 `cordis_inspect` 确认真实接口：

| 待查证项 | 位置 | 查证方式 |
|---|---|---|
| systemPrompt section 的真实插件名与 config | preset/README.md 的 §2/§3 | `cordis_inspect what:"services" name:"systemPrompt"` |
| SkillRegistration 确切字段 | src/host/host-plugin.js | 查 skills 服务 + 现有 skill 注册实例 |
| SkillProvider 契约（list/get） | src/host/host-plugin.js | 查 skills.registerProvider |
| 宿主组合如何加载插件（patch 还是 package） | src/host/host-plugin.js | 查 editing-cordis-compositions 技能 |
| view 稀疏投影的精确契约 | src/projection/projections.js | 查 sessionProjections + dsh-goal 的 view 实现 |

## 下一步（P0 收尾后 → P1）

1. 完成待查证项，把骨架升级为可挂载代码
2. `agentPresets.copy('cordis', 'wuji')` 建阿极 preset
3. 宿主插件写进 `cordis.patch.yml`（需确认加载方式）
4. `standingKeyFor` 验证 + 真实会话验证
5. 之后进入 P1（参谋部调度）

## 诚实说明（铁律五）

P0 当前是【骨架】不是【可运行】。所有"待查证项"是真实的——我没有臆断接口，而是明确标出"这里要用 cordis_inspect 查证"。这是符合铁律七（凡事有背书）的做法。
