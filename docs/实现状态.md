# dsh-wuji-legion 实现状态

> 私有开发版本。更新时间：2026-08-15。

| 阶段 | 已实现 |
|---|---|
| P0 | 阿极用户 preset、十一条铁律、阿极内置客观判断、能力目录、需求/任务/官员建议稀疏投影 |
| P1 | 参谋部 Host 服务、结构化任务规划、DAG/依赖校验、官方 subagents 派发、任务状态闭环 |
| P2 | 域主帅按域/能力档位选择路线；主帅不做第二调度器 |
| P3 | 独立官员 MoE 单职权建议；用户明确请求才生成隔离会审合同 |
| P4 | core/working/episodic 有界记忆；互补矩阵、行为探针、版本化晋升 |
| P5 | telemetry 投影、结构化反馈、当前 Session 状态摘要 |

## 当前宿主资源

- 投影：`wuji.requirement`、`wuji.task`、`wuji.officer-advice`、`wuji.memory`、`wuji.evolution`、`wuji.telemetry`
- Host 服务：`wujiStaff.validatePlan()`、`plan()`、`readyTasks()`
- 工具：`wuji_staff_plan`、`wuji_staff_dispatch`、`wuji_commander_select`、`wuji_officer_advise`、`wuji_council_plan`、`wuji_memory_write`、`wuji_evolution_propose`、`wuji_evolution_promote`、`wuji_feedback_record`、`wuji_status`
- 能力目录：8 项（飞书文档/表格/IM、HTML/PPTX 演示、开发、搜索、写作）
- 情报官入口：`wuji_intelligence_search`，调用 DSH 原生 `web.search`；情报官只收集，参谋部负责分析
- 客观判断：阿极内置，不再作为独立官员职权
- J-Space：不安装插件，仅蒸馏工作集限制、目标保持、中间桥接和自我监测，复用现有任务表/记忆/参谋部
- 图像输入：gpt-5.6 / gpt-5.6-sol / gpt-5.6-terra 已声明 `input: [text, image]`；非视觉模型继续使用明确拒绝或 `read_image`/OCR 回退，不伪装识图成功
- 冷恢复稳定性：所有 projection schema 已做 cold-restore 回归测试；曾修复 `wuji.telemetry.task` 漏包 `z.object()` 导致的重启历史加载失败

## 验证证据

完整宿主测试套件共 9 个测试文件：

- 投影折叠：12 项通过
- 稀疏 view/schema：6 项通过
- 参谋部规划与派发：通过
- 主帅选择：通过
- 官员与显式会审：通过
- 记忆与进化：通过
- 可观测性与反馈：通过
- 全套结果：`failures=0`

## 尚未冒充完成的内容

1. 当前包以本地 profile 插件形式安装；已提供 `scripts/install-local-profile.ps1`，正式 npm 发布仍待公开版收口。
2. 真实跨 provider 的端到端任务仍需在新重启实例中执行；单元与宿主注册测试已通过。
3. DSH Desktop 使用 `profiles/desktop`，不是 `profiles/web`；宿主 patch 已同步到实际 Desktop profile。
4. GitHub Pages 要等私库正式公开后才能启用。
5. GitHub 上传前必须执行敏感信息脱敏检查；公开版只保留通用配置和示例。
6. 公开发布前仍需版本号、CHANGELOG、贡献指南和正式安装文档。
