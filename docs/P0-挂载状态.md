# P0 宿主地基 · 挂载状态（v0.2）

> 日期：2026-08-15
> 状态：P0 骨架已落地，宿主插件已安装并写入 patch，**待 DSH 重启验证**。

---

## 一、已完成（真正落地）

| 交付物 | 位置 | 状态 |
|---|---|---|
| 阿极 preset | `~/.dsh/.agent-presets/wuji/` | ✅ 已创建（copy cordis + 改 persona：阿极身份 + 八条铁律 + 白帽义务 + goal契约） |
| 宿主插件包 | `~/.dsh/profiles/node_modules/@wuji/dsh-wuji-host/` | ✅ 已安装（与仓库 `packages/wuji-host/` 一致，hash 核对通过） |
| 宿主 patch | `~/.dsh/profiles/web/cordis.patch.yml` | ✅ 已写入 `- insert: { id: wuji-host, name: '@wuji/dsh-wuji-host' }` |
| 投影单元逻辑 | `packages/wuji-host/lib/projection.js` | ✅ 12 项测试全过（结构/新增/更新/激活/状态/证据/决策） |

## 二、设计要点（参考已验证源码）

- **投影注册模式**：采用 `dsh-goal` 源码验证的 `ctx.inject(['sessionProjections'], ...)` 模式（不是臆想签名）
- **插件导出**：Cordis 通过 package.json `main` 加载 default 导出（`{ name, apply }` 普通插件对象）
- **三张表**：需求表（阿极）、任务表（参谋部，甘特图式）、官员建议表（官员零修改权）
- **稀疏投影**：view 目前 `s => s`，精确"框架+当前项"待 view 契约查证后实现

## 三、待验证/待配置（诚实说明）

| 项 | 说明 | 如何做 |
|---|---|---|
| **DSH 重启验证** | patch 和插件在 DSH **重启后**才加载；当前运行的 harness 用的是旧组合 | 重启 DSH（我无法替您做） |
| **WUJI_REGISTRY_PATH** | 技能注册依赖此环境变量指向 `skills/capability-registry.json`；未配置时注册空库（不阻断启动） | 设置环境变量或在宿主启动脚本里导出 |
| **SkillRegistration 字段** | `skills.register({ name, description })` 用了最简字段；完整契约待挂载后 inspect 验证 | DSH 重启后用 `cordis_inspect` 查 skills 服务 |
| **view 稀疏投影** | 目前透传整表；"框架+当前项"待对照 view 契约实现 | 查 sessionProjections 真实 view 语义 |

## 四、风险与边界（铁律六）

1. **改了 web profile 的 patch**——这是用户正在运行的 harness 配置。DSH 重启后插件生效；若插件加载失败，DSH 会报错（patch 引用了未安装的包会 fail），但我们的包已安装、逻辑已验证，失败风险低。
2. **未跑 pnpm install**——包装是手动复制进 node_modules，不是 pnpm 依赖树的一部分。若 DSH 重启时清 node_modules，包装会丢。后续应把包加入 profile 的 package.json 依赖（或文档说明重装方式）。

## 五、下一步

1. 用户重启 DSH
2. 验证：`cordis_inspect` 看到 `wuji.requirement` / `wuji.task` / `wuji.officer-advice` 三个投影 key
3. 验证：skills 目录出现 8 条能力（若 WUJI_REGISTRY_PATH 配置正确）
4. 若通过 → P0 完成；进入 P1（参谋部调度）
